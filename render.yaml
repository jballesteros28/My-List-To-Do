services:
  - type: web
    name: my-list-to-do
    env: python
    plan: free
    buildCommand: poetry install
    startCommand: poetry run uvicorn app.main:app --host 0.0.0.0 --port 10000
    autoDeploy: true